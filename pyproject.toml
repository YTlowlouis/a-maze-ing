title = "TOML File for Maze Generator"

[project]
name = "mazegen"
version = "1.0.0"
description = "Maze generator"
authors = [{name = "loboehm, fchaput"}]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"